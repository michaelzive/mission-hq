services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: missionhq
      POSTGRES_USER: missionhq
      POSTGRES_PASSWORD: missionhq
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
