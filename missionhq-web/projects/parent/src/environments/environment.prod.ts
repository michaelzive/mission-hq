export const environment = {
  production: true,
  /** Set this to wherever the backend is reachable from the tablets, e.g. 'https://api.missionhq.example/api/v1'. Relative works if the API is served from the same origin. */
  apiBaseUrl: '/api/v1',
};
